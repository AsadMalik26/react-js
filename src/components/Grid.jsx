import React from "react";
import { Button } from "react-bootstrap";
let sx = { px: 3 };
const TheGrid = () => {
  return (
    <div>
      <Button variant="primary">This is from bootstrap</Button>
    </div>
  );
};

export default TheGrid;
